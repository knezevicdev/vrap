image: node:latest

stages:
  - build

build-checkout:
  only:
    - merge_requests
    changes:
      - apps/checkout/**
  stage: build
  script:
    - cd apps/checkout
    - npm install --quiet
    - npm run build