import React from 'react';

interface Props {
  viewModel: ViewModel;
}

const InitialPriceView: React.FC<Props> = ({ viewModel }) => {
  return (
    <div>this has like your price info</div>
  );
};

export default InitialPriceView;
