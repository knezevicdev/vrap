import React from 'react';
import ViewModel from './ViewModel';

interface Props {
  viewModel: ViewModel;
}

const ContactModule: React.FC<Props> = ({ viewModel }) => {
  return <div></div>;
};

export default ContactModule;
