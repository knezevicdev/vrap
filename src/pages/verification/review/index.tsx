import { observer } from 'mobx-react';
import { NextPage } from 'next';
import React from 'react';

const VerificationReview: NextPage = () => {
  return <div>Revuew page</div>;
};

export default observer(VerificationReview);
