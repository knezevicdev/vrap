mutation ACCEPT_REJECT_OFFER($offerId: String!, $accepted: Boolean!, $externalUserId: String) {
  acceptRejectOffer(offerId: $offerId, accepted: $accepted, externalUserId: $externalUserId) {
    offerStatus
  }
}
