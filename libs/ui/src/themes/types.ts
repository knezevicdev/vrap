export interface ThemeProps {
    theme: {
        typography: {
            family: {
                hero: string;
                title: string;
                body: string;
            };
        };
    };
}
